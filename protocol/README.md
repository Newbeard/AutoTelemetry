# Device protocol specifications

Reserved for future versioned schemas, compatibility fixtures, capability identifiers, and transport bindings. Architecture is defined in `docs/device-protocol-architecture.md`; implementation belongs in firmware and clients.

