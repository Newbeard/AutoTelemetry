# Firmware architecture

This tree is reserved for a future implementation of the modules defined in `docs/architecture.md`. The normalized telemetry core is the only shared data boundary; transports and outputs must not decode vehicle frames directly. No firmware implementation is authorized by this scaffold.

