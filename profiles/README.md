# Vehicle profiles

Canonical, reviewable declarative vehicle-profile sources will live here. Profiles declare support level, matching, CAN decode, diagnostic requests, validity, source priority, and metadata as defined in `docs/telemetry-data-model.md`; generated firmware artifacts do not become the source of truth.

